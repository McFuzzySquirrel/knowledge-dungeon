/**
 * Compatibility re-export. The renderer-neutral boss-floor rules moved to
 * `src/core/layout/bossRooms.ts`; this path is kept so existing first-party
 * and third-party imports keep resolving.
 */
export * from '@/core/layout/bossRooms';
