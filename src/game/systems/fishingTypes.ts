/**
 * Compatibility re-export. The renderer-neutral fishing catalog and types moved
 * to `src/core/fishing/fishingTypes.ts`; this path is kept so existing
 * first-party and third-party imports keep resolving.
 */
export * from '@/core/fishing/fishingTypes';
